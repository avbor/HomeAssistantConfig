#################################################
# Scripts for humidifiers
#################################################

set_smartmi_humidifier_speed:
  fields:
    fan_entity:
      description: "entity_id увлажнителя"
      example: "fan.xiaomi_miio_device"
    fan_speed:
      description: "Скорость работы увлажнителя"
      example: "Silent"
  sequence:
    service: fan.set_speed
    data_template:
      entity_id: "{{ fan_entity }}"
      speed: "{{ fan_speed }}"