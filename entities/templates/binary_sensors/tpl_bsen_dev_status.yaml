#################################################
# Device status sensors
#################################################
- binary_sensor:

  - name: Boiler Status
    device_class: heat
    state: "{{ (states('sensor.tplink_smartplug_01_current') | float) > 4 }}"
    icon: mdi:water-boiler

  - name: Fridge Status
    device_class: cold
    state: "{{ is_state('switch.gosund_sp111_04_switch', 'on') }}"
    icon: mdi:fridge-outline

  - name: Kettle Status
    device_class: heat
    state: "{{ (states('sensor.gosund_sp111_05_watt') | float) > 100 }}"
    icon: mdi:kettle

  - name: Dishwasher Status
    device_class: power
    state: "{{ (states('sensor.gosund_sp111_06_watt') | float) > 1 }}"
    icon: mdi:dishwasher

  - name: Washer Status
    device_class: power
    state: "{{ (states('sensor.tplink_smartplug_02_current_consumption') | float) > 5 }}"
    icon: mdi:washing-machine

  - name: Dryer Status
    device_class: power
    state: "{{ (states('sensor.gosund_sp111_09_watt') | float) > 5 }}"
    icon: mdi:tumble-dryer

  - name: Yandex Station Mini2 Gostinaia Status
    device_class: power
    state: "{{ not states('media_player.yandex_station_mini2_gostinaia') in ('paused', 'idle') }}"

  - name: Yandex Station Lite Detskaia Status
    device_class: power
    state: "{{ not states('media_player.yandex_station_lite_detskaia') in ('paused', 'idle') }}"

  - name: Bathroom Extractor Fan
    device_class: power
    state: "{{ is_state('fan.bathroom_extractor', 'on') }}"

  - name: Toilet Extractor Fan
    device_class: power
    state: "{{ is_state('fan.toilet_extractor', 'on') }}"